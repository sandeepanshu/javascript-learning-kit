// 45 minutes se start hua hai dekhna fir se

// let arr = [1, 4, 5, 7, 9, 6, 2];

// for (let i = 0; i < arr.length; i++) {
//   const element = arr[i];
// }

// let i = arr.length - 1;
// let key = arr[i];
// let j = i - 1;

// while (j >= 0 && arr[j] > key) {
//   arr[j + 1] = arr[j];
//   j--;
// }
// arr[j + 1] = key;
// console.log(arr);

// Worst case time complexity of insertion sort
// O(n^2)
// Best case time complexity = omega(n)
// space complexity = O(1)
// inplace ? Yes
// stable ? Yes
